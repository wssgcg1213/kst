import utils, {getFiles} from './utils'
import cache from './cache'

const srcFiles = getFiles(srcPath)

export default class {

  constructor (...args) {
    this.init(...args)
  } 

  init () {
    
  }
}
